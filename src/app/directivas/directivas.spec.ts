import { Directivas } from './directivas.directive';

describe('Directivas', () => {
  it('should create an instance', () => {
    const directive = new Directivas();
    expect(directive).toBeTruthy();
  });
});
